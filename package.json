{
    "name": "sky-hopper",
    "private": true,
    "devDependencies": {
        "esbuild": "^0.25.0"
    },
    "scripts": {
        "build": "node scripts/build.mjs",
        "dev": "python3 -m http.server 8080"
    },
    "dependencies": {
        "phaser": "^3.70.0"
    }
}
